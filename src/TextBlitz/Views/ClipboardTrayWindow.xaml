<Window x:Class="TextBlitz.Views.ClipboardTrayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="TextBlitz"
        Width="380" Height="560"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize"
        Deactivated="Window_Deactivated">

    <Window.Resources>
        <ResourceDictionary>
            <!-- Colors -->
            <Color x:Key="BackgroundColor">#1E1E2E</Color>
            <Color x:Key="SurfaceColor">#313244</Color>
            <Color x:Key="TextColor">#CDD6F4</Color>
            <Color x:Key="SubTextColor">#A6ADC8</Color>
            <Color x:Key="AccentColor">#89B4FA</Color>
            <Color x:Key="SuccessColor">#A6E3A1</Color>
            <Color x:Key="WarningColor">#F9E2AF</Color>
            <Color x:Key="ErrorColor">#F38BA8</Color>

            <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}" />
            <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}" />
            <SolidColorBrush x:Key="TextBrush" Color="{StaticResource TextColor}" />
            <SolidColorBrush x:Key="SubTextBrush" Color="{StaticResource SubTextColor}" />
            <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}" />
            <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}" />
            <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}" />
            <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource ErrorColor}" />

            <!-- Base button style -->
            <Style x:Key="BaseButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
                <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Padding" Value="12,6" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    CornerRadius="4"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#45475A" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#585B70" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Accent button style -->
            <Style x:Key="AccentButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseButtonStyle}">
                <Setter Property="Background" Value="{StaticResource AccentBrush}" />
                <Setter Property="Foreground" Value="#1E1E2E" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background">
                            <Setter.Value>
                                <SolidColorBrush Color="#B4D0FB" />
                            </Setter.Value>
                        </Setter>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- Search TextBox style -->
            <Style x:Key="SearchTextBoxStyle" TargetType="TextBox">
                <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
                <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
                <Setter Property="CaretBrush" Value="{StaticResource TextBrush}" />
                <Setter Property="BorderBrush" Value="#45475A" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Padding" Value="8,6" />
                <Setter Property="FontSize" Value="13" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6">
                                <Grid>
                                    <ScrollViewer x:Name="PART_ContentHost" Margin="0" />
                                    <TextBlock Text="Search clips..."
                                               Foreground="{StaticResource SubTextBrush}"
                                               Margin="10,7,0,0"
                                               IsHitTestVisible="False"
                                               FontSize="13">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value="">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsFocused" Value="True">
                                    <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Toggle RadioButton style (for formatting modes) -->
            <Style x:Key="ToggleRadioStyle" TargetType="RadioButton">
                <Setter Property="Foreground" Value="{StaticResource SubTextBrush}" />
                <Setter Property="FontSize" Value="11" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="RadioButton">
                            <Border x:Name="Bd"
                                    Background="{StaticResource SurfaceBrush}"
                                    CornerRadius="4"
                                    Padding="10,5"
                                    BorderBrush="#45475A"
                                    BorderThickness="1">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#45475A" />
                                    <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                                    <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#3A3A4E" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Clipboard item ListBox style -->
            <Style x:Key="ClipboardListBoxStyle" TargetType="ListBox">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
                <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True" />
                <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling" />
            </Style>

            <Style x:Key="ClipboardListBoxItemStyle" TargetType="ListBoxItem">
                <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
                <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
                <Setter Property="Margin" Value="0,0,0,4" />
                <Setter Property="Padding" Value="10,8" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ListBoxItem">
                            <Border x:Name="Bd"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="6"
                                    Padding="{TemplateBinding Padding}"
                                    Margin="{TemplateBinding Margin}">
                                <ContentPresenter />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#3A3A4E" />
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#45475A" />
                                    <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                                    <Setter TargetName="Bd" Property="BorderThickness" Value="1" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Tab control styling -->
            <Style x:Key="DarkTabControlStyle" TargetType="TabControl">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
            </Style>

            <Style x:Key="DarkTabItemStyle" TargetType="TabItem">
                <Setter Property="Foreground" Value="{StaticResource SubTextBrush}" />
                <Setter Property="FontSize" Value="13" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TabItem">
                            <Border x:Name="Bd"
                                    Padding="16,8"
                                    Background="Transparent"
                                    BorderThickness="0,0,0,2"
                                    BorderBrush="Transparent">
                                <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                                    <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#2A2A3C" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Pin toggle button style -->
            <Style x:Key="PinToggleStyle" TargetType="ToggleButton">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="{StaticResource SubTextBrush}" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="Width" Value="28" />
                <Setter Property="Height" Value="28" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ToggleButton">
                            <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                <TextBlock Text="&#x1F4CC;"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           FontSize="12"
                                           Foreground="{TemplateBinding Foreground}" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#45475A" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Border Background="{StaticResource BackgroundBrush}"
            CornerRadius="12"
            BorderBrush="#45475A"
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.5" Color="Black" />
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />  <!-- Header -->
                <RowDefinition Height="Auto" />  <!-- Search -->
                <RowDefinition Height="Auto" />  <!-- Formatting mode -->
                <RowDefinition Height="*" />      <!-- Content (tabs) -->
                <RowDefinition Height="Auto" />  <!-- Footer -->
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Padding="14,10" Background="Transparent"
                    MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="TextBlitz"
                                   Foreground="{StaticResource AccentBrush}"
                                   FontSize="16" FontWeight="Bold"
                                   VerticalAlignment="Center" />
                        <TextBlock Text="Clipboard"
                                   Foreground="{StaticResource SubTextBrush}"
                                   FontSize="11" Margin="8,0,0,0"
                                   VerticalAlignment="Center" />
                    </StackPanel>
                    <Button Content="&#x2715;"
                            HorizontalAlignment="Right"
                            Style="{StaticResource BaseButtonStyle}"
                            Padding="6,2"
                            FontSize="14"
                            Command="{Binding CloseCommand}"
                            Click="CloseButton_Click" />
                </Grid>
            </Border>

            <!-- Search -->
            <Border Grid.Row="1" Padding="14,0,14,8">
                <TextBox Style="{StaticResource SearchTextBoxStyle}"
                         Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" />
            </Border>

            <!-- Formatting Mode -->
            <Border Grid.Row="2" Padding="14,0,14,8">
                <StackPanel>
                    <TextBlock Text="Formatting Mode"
                               Foreground="{StaticResource SubTextBrush}"
                               FontSize="10" Margin="0,0,0,4" />
                    <UniformGrid Columns="3" Rows="1">
                        <RadioButton Content="Keep Original"
                                     Style="{StaticResource ToggleRadioStyle}"
                                     IsChecked="{Binding IsKeepOriginalMode}"
                                     Margin="0,0,3,0" />
                        <RadioButton Content="Destination"
                                     Style="{StaticResource ToggleRadioStyle}"
                                     IsChecked="{Binding IsDestinationMode}"
                                     Margin="0,0,3,0" />
                        <RadioButton Content="Merge (Beta)"
                                     Style="{StaticResource ToggleRadioStyle}"
                                     IsChecked="{Binding IsMergeMode}" />
                    </UniformGrid>
                </StackPanel>
            </Border>

            <!-- Tab Content -->
            <TabControl Grid.Row="3"
                        Style="{StaticResource DarkTabControlStyle}"
                        SelectedIndex="{Binding SelectedTabIndex}"
                        Margin="14,0">
                <TabItem Header="History" Style="{StaticResource DarkTabItemStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <ListBox Grid.Row="0"
                                 Style="{StaticResource ClipboardListBoxStyle}"
                                 ItemContainerStyle="{StaticResource ClipboardListBoxItemStyle}"
                                 ItemsSource="{Binding FilteredClipboardItems}"
                                 SelectedItem="{Binding SelectedClipboardItem}"
                                 SelectionMode="Extended"
                                 Margin="0,8,0,0">
                            <ListBox.InputBindings>
                                <MouseBinding Gesture="LeftDoubleClick"
                                              Command="{Binding PasteItemCommand}" />
                            </ListBox.InputBindings>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                            <TextBlock Text="{Binding PlainText}"
                                                       Foreground="{StaticResource TextBrush}"
                                                       FontSize="12"
                                                       TextTrimming="CharacterEllipsis"
                                                       MaxHeight="38"
                                                       LineHeight="19"
                                                       LineStackingStrategy="BlockLineHeight" />
                                            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                                <TextBlock Text="{Binding Timestamp, StringFormat='{}{0:HH:mm}'}"
                                                           Foreground="{StaticResource SubTextBrush}"
                                                           FontSize="10" />
                                                <TextBlock Text=" - "
                                                           Foreground="{StaticResource SubTextBrush}"
                                                           FontSize="10"
                                                           Visibility="{Binding SourceApp, Converter={x:Static StringToVisibilityConverter.Instance}, FallbackValue=Collapsed}" />
                                                <TextBlock Text="{Binding SourceApp}"
                                                           Foreground="{StaticResource SubTextBrush}"
                                                           FontSize="10" />
                                            </StackPanel>
                                        </StackPanel>

                                        <ToggleButton Grid.Column="1"
                                                      Style="{StaticResource PinToggleStyle}"
                                                      IsChecked="{Binding IsPinned}"
                                                      VerticalAlignment="Center" />

                                        <Grid.ContextMenu>
                                            <ContextMenu Background="{StaticResource SurfaceBrush}"
                                                         Foreground="{StaticResource TextBrush}"
                                                         BorderBrush="#45475A">
                                                <MenuItem Header="Paste"
                                                          Command="{Binding DataContext.PasteItemCommand, Source={x:Reference HistoryListRef}}" />
                                                <MenuItem Header="Copy"
                                                          Command="{Binding DataContext.CopyItemCommand, Source={x:Reference HistoryListRef}}" />
                                                <Separator />
                                                <MenuItem Header="Pin/Unpin"
                                                          Command="{Binding DataContext.TogglePinCommand, Source={x:Reference HistoryListRef}}" />
                                                <MenuItem Header="Delete"
                                                          Command="{Binding DataContext.DeleteItemCommand, Source={x:Reference HistoryListRef}}" />
                                            </ContextMenu>
                                        </Grid.ContextMenu>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <!-- Save as List button (visible when items are selected) -->
                        <Button Grid.Row="1"
                                Content="Save as List..."
                                Style="{StaticResource AccentButtonStyle}"
                                Command="{Binding SaveAsListCommand}"
                                Margin="0,8,0,4"
                                Visibility="{Binding HasSelectedItems, Converter={x:Static BoolToVisibilityConverter.Instance}, FallbackValue=Collapsed}" />
                    </Grid>
                </TabItem>

                <TabItem Header="Lists" Style="{StaticResource DarkTabItemStyle}" x:Name="HistoryListRef">
                    <Grid Margin="0,8,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <!-- Back button (visible when viewing a list) -->
                        <Button Grid.Row="0"
                                Style="{StaticResource BaseButtonStyle}"
                                Command="{Binding BackToListsCommand}"
                                HorizontalAlignment="Left"
                                Margin="0,0,0,8"
                                Visibility="{Binding IsViewingList, Converter={x:Static BoolToVisibilityConverter.Instance}, FallbackValue=Collapsed}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#x2190;" Margin="0,0,6,0" />
                                <TextBlock Text="Back to Lists" />
                            </StackPanel>
                        </Button>

                        <!-- Lists view -->
                        <ListBox Grid.Row="1"
                                 Style="{StaticResource ClipboardListBoxStyle}"
                                 ItemContainerStyle="{StaticResource ClipboardListBoxItemStyle}"
                                 ItemsSource="{Binding DisplayedListItems}"
                                 Visibility="{Binding IsViewingList, Converter={x:Static BoolToVisibilityConverter.Instance}, FallbackValue=Collapsed}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding PlainText}"
                                               Foreground="{StaticResource TextBrush}"
                                               FontSize="12"
                                               TextTrimming="CharacterEllipsis" />
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <ListBox Grid.Row="1"
                                 Style="{StaticResource ClipboardListBoxStyle}"
                                 ItemContainerStyle="{StaticResource ClipboardListBoxItemStyle}"
                                 ItemsSource="{Binding SavedLists}"
                                 SelectedItem="{Binding SelectedList}"
                                 Visibility="{Binding IsViewingList, Converter={x:Static InverseBoolToVisibilityConverter.Instance}, FallbackValue=Visible}">
                            <ListBox.InputBindings>
                                <MouseBinding Gesture="LeftDoubleClick"
                                              Command="{Binding OpenListCommand}" />
                            </ListBox.InputBindings>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel>
                                        <TextBlock Text="{Binding Name}"
                                                   Foreground="{StaticResource TextBrush}"
                                                   FontSize="13" FontWeight="SemiBold" />
                                        <TextBlock Text="{Binding CreatedAt, StringFormat='{}{0:MMM dd, yyyy}'}"
                                                   Foreground="{StaticResource SubTextBrush}"
                                                   FontSize="10" Margin="0,2,0,0" />
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </TabItem>
            </TabControl>

            <!-- Footer -->
            <Border Grid.Row="4" Padding="14,6">
                <Grid>
                    <TextBlock Text="{Binding StatusText}"
                               Foreground="{StaticResource SubTextBrush}"
                               FontSize="10"
                               VerticalAlignment="Center" />
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="&#x2699;"
                                Style="{StaticResource BaseButtonStyle}"
                                Command="{Binding OpenSettingsCommand}"
                                Padding="6,2" FontSize="14"
                                ToolTip="Settings" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
